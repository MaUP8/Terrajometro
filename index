<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Terrajómetro</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
      background: #f0f0f0;
      color: #333;
      margin: 0;
      padding: 0;
    }

    h1 {
      margin-top: 50px;
      font-size: 2.5em;
      color: #ff5733;
    }

    #container {
      margin-top: 50px;
    }

    .gauge {
      position: relative;
      width: 300px;
      height: 150px;
      margin: 50px auto;
    }

    .gauge .dial {
      fill: #ddd;
    }

    .gauge .needle {
      fill: #ff5733;
    }

    .result {
      display: none;
      margin-top: 20px;
      font-size: 1.5em;
      color: #ff5733;
    }

    button {
      padding: 10px 20px;
      font-size: 1em;
      background-color: #ff5733;
      color: #fff;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    button:hover {
      background-color: #ff6f4d;
    }
  </style>
</head>
<body>
  <h1>¡Terrajómetro!</h1>
  <p>Subí una foto para medir tu nivel de terraja</p>
  <div id="container">
    <input type="file" id="upload" accept="image/*">
    <div class="gauge">
      <svg viewBox="0 0 300 150">
        <path d="M10,140 A140,140 0 0,1 290,140" class="dial" stroke="black" stroke-width="2" fill="#eee"></path>
        <line x1="150" y1="140" x2="150" y2="50" class="needle" stroke-width="5"></line>
      </svg>
    </div>
    <div class="result" id="result">
      Tu nivel de terraja es: <span id="terraja-score"></span>%
      <p id="message"></p>
    </div>
    <button id="start-btn">Medir nivel</button>
  </div>

  <script>
    document.getElementById("start-btn").addEventListener("click", function () {
      const needle = document.querySelector(".needle");
      const resultDiv = document.getElementById("result");
      const terrajaScore = document.getElementById("terraja-score");
      const message = document.getElementById("message");

      // Animación del velocímetro
      let rotation = 0;
      const interval = setInterval(() => {
        if (rotation >= 120) { // Detener al máximo
          clearInterval(interval);

          // Mostrar resultado
          const score = Math.floor(Math.random() * 21) + 80; // Siempre entre 80% y 100%
          terrajaScore.textContent = score;
          message.textContent =
            score > 90
              ? "¡Nivel tan alto que podrías patentar las ojotas fluorescentes!"
              : "¡Te falta poquito para el termo de piel!";
          resultDiv.style.display = "block";
        } else {
          rotation += 5;
          needle.style.transform = `rotate(${rotation - 90}deg)`; // Ajustar desde -90 grados
          needle.style.transformOrigin = "50% 140px";
        }
      }, 50);
    });
  </script>
</body>
</html>
